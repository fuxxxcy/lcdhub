import styled from "styled-components";

const Button = styled.a`
  display: block;
  width: 90%;
  height: 50px;
  border: 3px solid var(--primarly);
  color: var(--text);
  border-radius: 2em;
`;

export {
  Button
};