type DiscordData = {
  accent_color: string,
  avatar: string,
  avatar_decoration: string | null,
  banner: string | null,
  banner_color: string | null,
  discriminator: string,
  flags: number | number[],
  global_name: string,
  id: string,
  locale: string,
  mfa_enabled: boolean,
  premium_type: number,
  public_flags: number,
  username: string
};
